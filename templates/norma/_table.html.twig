<table class="table table-hover m-table m-table--head-separator-brand" id="norma_table">
    <thead>
    <tr>
        <th style="width: 10%">
            #
        </th>
        <th>
            Título
        </th>
        <th style="width: 10%">
            Fecha
        </th>
        <th>
        </th>
    </tr>
    </thead>
    <tbody>
    {%set i=1%}
    {% for norma in normas %}
        <tr>
            <td>{{ i }}</td>
            <td>{{ norma.id.titulo }}</td>
            <td>{{ norma.id.fechaCaptacion|date('d-m-Y') }}</td>
            <td>
                <ul class="m-nav m-nav--inline m--pull-right">
                    <li class="m-nav__item">
                        <a class="btn btn-secondary btn-sm" href="{{ path('norma_show', { 'id': norma.id.id }) }}">
                            <i class="flaticon-eye"></i> Visualizar</a>
                    </li>
                    {% if esGestor==true %}
                    <li class="m-nav__item">
                        <a class="btn btn-info btn-sm" href="{{ path('norma_edit', { 'id': norma.id.id }) }}">
                            <i class="flaticon-edit-1"></i> Editar</a>
                    </li>
                    <li class="m-nav__item">
                        <a class="btn btn-danger btn-sm  eliminar_norma" data-href="{{ path('norma_delete', { 'id': norma.id.id }) }}" data-csrf="{{ csrf_token('delete' ~ norma.id.id) }}">
                            <i class="flaticon-delete-1"></i> Eliminar</a>
                    </li>
                    {% endif %}
                </ul>
            </td>
        </tr>
        {%set i=1+i%}
    {% endfor %}
    </tbody>
    <tfoot>
    <tr>
        <th>
            #
        </th>
        <th>
            Título
        </th>
        <th>
            Fecha
        </th>
        <th>
        </th>
    </tr>
    </tfoot>
</table>
