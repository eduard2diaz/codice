<table class="table table-hover m-table m-table--head-separator-brand" id="institucion_table">
    <thead>
    <tr>
        <th>
            #
        </th>
        <th>
            Nombre
        </th>
        <th>
            País
        </th>
        <th>
            Ministerio
        </th>
        <th>
        </th>
    </tr>
    </thead>
    <tbody>
    {%set i=1%}
    {% for institucion in institucions %}
        <tr>
            <td>{{ i }}</td>
            <td>{{ institucion.nombre }}</td>
            <td>{{ institucion.pais.nombre }}</td>
            <td>{{ institucion.ministerio.nombre }}</td>
            <td>
                <ul class="m-nav m-nav--inline m--pull-right">
                    <li class="m-nav__item">
                        <a class="btn btn-info btn-sm edicion" data-href="{{ path('institucion_edit', { 'id': institucion.id }) }}">
                            <i class="flaticon-edit-1"></i> Editar</a>
                    </li>
                    <li class="m-nav__item">
                        <a class="btn btn-danger btn-sm  eliminar_institucion" data-href="{{ path('institucion_delete', { 'id': institucion.id }) }}" data-csrf="{{ csrf_token('delete' ~ institucion.id) }}">
                            <i class="flaticon-delete-1"></i> Eliminar</a>
                    </li>
                </ul>
            </td>
        </tr>
        {%set i=1+i%}
    {% endfor %}
    </tbody>
    <tfoot>
    <tr>
        <th>
            #
        </th>
        <th>
            Nombre
        </th>
        <th>
            País
        </th>
        <th>
            Ministerio
        </th>
        <th>
        </th>
    </tr>
    </tfoot>
</table>
