<table class="table table-hover m-table m-table--head-separator-brand" id="tesis_table">
    <thead>
    <tr>
        <th>
            #
        </th>
        <th>
            Titulo
        </th>
        <th>
            Fecha
        </th>
        <th>
        </th>
    </tr>
    </thead>
    <tbody>
    {%set i=1%}
    {% for tesis in tesiss %}
        <tr>
            <td>{{ i }}</td>
            <td>{{ tesis.id.titulo }}</td>
            <td>{{ tesis.id.fechaCaptacion|date('d-m-Y') }}</td>
            <td>
                <ul class="m-nav m-nav--inline m--pull-right">
                    <li class="m-nav__item">
                        <a class="btn btn-secondary btn-sm" href="{{ path('tesis_show', { 'id': tesis.id.id }) }}">
                            <i class="flaticon-eye"></i> Ver</a>
                    </li>
                    <li class="m-nav__item">
                        <a class="btn btn-info btn-sm" href="{{ path('tesis_edit', { 'id': tesis.id.id }) }}">
                            <i class="flaticon-edit-1"></i> Editar</a>
                    </li>
                    <li class="m-nav__item">
                        <a class="btn btn-danger btn-sm  eliminar_tesis" data-href="{{ path('tesis_delete', { 'id': tesis.id.id }) }}">
                            <i class="flaticon-delete-1"></i> Eliminar</a>
                    </li>
                </ul>
            </td>
        </tr>
        {%set i=1+i%}
    {% endfor %}
    </tbody>
    <tfoot>
    <tr>
        <th>
            #
        </th>
        <th>
            Titulo
        </th>
        <th>
            Fecha
        </th>
        <th>
        </th>
    </tr>
    </tfoot>
</table>
